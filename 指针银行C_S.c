基于进程间通信或C/S架构的银行ATM系统
分为两大模块：
    客户端(Client)
        两层菜单：
        1、开户、销户、登录、解锁
            开户：输入名字、身份证号、设置6位密码、输入金额、自动生成账号，让服务器让每个用户生成一个账号文件(一个文件只保存一个用户的信息，账号作为文件名)
            销户：输入账号、密码、身份证号，如果有则删除账号文件
            登录：输入账号、密码，三次密码输错锁定账户
        解锁：输入账号、身份证号解锁
        2、存钱、取钱、转账、查询、改密
            存钱：输入金额
            取钱：输入金额
            转账：输入转账账户、金额
            查询：不需要输入
            改密：输入原密码和新密码
    服务器(Server)
        创建9个子进程各自负责上面的9个功能
        每个子进程根据消息类型来接收消息
        根据客户端的进程号来发送消息
    推荐技术栈：
        vfork+exec+消息队列
        两种结构体：
            客户端发服务器的消息包结构体
            服务器发客户端的消息包结构体